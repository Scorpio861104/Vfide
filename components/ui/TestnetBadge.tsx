'use client';

// Network-agnostic components - removed testnet-specific UI
// App works identically on all networks based on wallet connection

/**
 * Legacy component - no longer displays testnet-specific UI
 * Kept for backwards compatibility
 */
export function TestnetBadge() {
  return null;
}

/**
 * Legacy component - no longer displays testnet-specific UI
 * Kept for backwards compatibility
 */
export function TestnetCornerBadge() {
  return null;
}
