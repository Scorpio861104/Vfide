import { describe, it } from '@jest/globals';

// app/merchant-setup/ no longer exists in the repo. Test is preserved as a
// documented stub so historical CI references stay green.
describe.skip('merchant-setup page (removed)', () => {
  it('placeholder', () => undefined);
});
