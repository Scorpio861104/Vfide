--- /home/claude/v19-12-baseline/Vfide-main/components/ui/AddressDisplay.tsx	2026-05-09 01:07:29.000000000 +0000
+++ /home/claude/v19-12-rollup/modified-files/components/ui/AddressDisplay.tsx	2026-05-09 01:14:05.650883899 +0000
@@ -8,6 +8,11 @@
 // F-FE-005 FIX: ENS profile URLs are user-supplied. Validate before rendering
 // as <a href> to prevent javascript:/data: URI XSS.
 import { isExternalUrlSafe } from '@/lib/security/urlValidation';
+// v19.10 BCOMPAT-1 FIX: cross-browser clipboard helper. Without this,
+// copy buttons silently fail in iOS in-app browsers (Twitter,
+// Facebook, Instagram, WhatsApp, Telegram, Discord) — high-impact UX
+// failure for any user landing via a shared link.
+import { copyToClipboardSafe } from '@/lib/clipboardSafe';
 
 export interface ENSProfile {
   address: string;
@@ -194,13 +199,16 @@
   const { avatar: avatarSize, text: textSize, gap } = sizeConfig[size];
 
   const handleCopy = async () => {
-    try {
-      await navigator.clipboard.writeText(address);
+    // v19.10 BCOMPAT-1 FIX: only flag "copied" on actual success.
+    // Helper handles in-app browser fallback path (legacy execCommand).
+    const ok = await copyToClipboardSafe(address);
+    if (ok) {
       setCopied(true);
       setTimeout(() => setCopied(false), 2000);
-    } catch {
-      // copy failed; button stays in un-copied state, user can copy manually
     }
+    // If both clipboard paths failed, leave the button in un-copied
+    // state. User can long-press the visible address text to copy
+    // manually (the address row supports text selection).
   };
 
   const displayText = truncate 
