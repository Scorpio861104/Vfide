- [x] N-C1 — `vfide_app` DB role grants are incomplete; RLS will brick or be bypassed in production
- [x] N-C2 — `VaultHub.executeRecoveryRotation` has a 72h `RECOVERY_CHALLENGE_DELAY` with no challenge mechanism
- [x] N-C3 — Step-up authentication for high-risk payments is satisfied by a client-controlled header
- [x] N-C4 — `VFIDEToken._transfer` checks fraud status of the *vault contract*, never the user — fraud-escrow is dead code for vault-mediated transfers (HIGHEST IMPACT)
- [x] N-C5 — SIWE challenge `domain` built from `Host` header (re-verify after C-12/C-15)
- [x] N-C6 — `DAOTimelock.execute` never calls back to `DAO.markExecuted`; proposals stay `queued=true, executed=false` forever
- [x] N-H1 — `FraudRegistry.clearFlag` unbounded loop, ~500 transfers per call
- [x] N-H2 — `FraudRegistry.clearFlag` doesn't reset complaint state → 1-vote re-flag griefing
- [x] N-H3 — Escrow recipient is captured at send-time as the *destination vault address*; vault rotation in the 30-day window orphans funds
- [x] N-H4 — `releaseEscrow` reverts forever if `systemExempt` flag is later removed
- [x] N-H5 — `users_read_authenticated` policy lets every authenticated user read every column of `users`
- [x] N-H6 — `CardBoundVault.executeRecoveryRotation` leaves dangerous queued state for the new admin to inadvertently apply
- [x] N-H7 — Merchant `pending_confirmation` state can be set by anyone with the public payment link
- [x] N-H8 — CSV export is vulnerable to formula injection in Excel/Sheets/Numbers
- [x] N-H9 — `MerchantPortal.processPayment` (legacy public function) skips fraud-status checks on customer and merchant
- [x] N-H10 — `VaultHub.executeRecoveryRotation` doesn't clear pending sensitive state on the vault
- [x] N-H11 — `MerchantPortal.initiateRefund` lets a merchant fill any customer's refund-history cap, disabling future refunds
- [x] N-H12 — `SubscriptionManager.processPayment` allows merchant to auto-cancel a subscription via 3 failed payments in one block
- [x] N-H13 — `VFIDEBridge` no automatic refund when destination liquidity exhausts
- [x] N-H14 — `VFIDECommerce.CommerceEscrow.dispute` can grief a merchant into auto-suspension with 3 minimal-cost disputes
- [x] N-H15 — `VFIDECommerce.CommerceEscrow.release`/`refund` send to vault addresses captured at `open()` — vault rotation in flight orphans funds
- [x] N-H16 — `OwnerControlPanel` exposes 3 always-reverting recovery functions
- [x] N-H17 — `MerchantPortal.processPayment` (legacy entry) skips `_checkFraudStatus`
- [x] N-H18 — `VaultRegistry.setEmailRecovery` and `setPhoneRecovery` accept un-salted hashes, defeating H-48
- [x] N-H19 — `OwnerControlPanel.vault_freezeVault` is non-functional but advertised as a working freeze
- [x] N-H20 — `app/api/auth/route.ts` returns the JWT in the JSON response body in addition to the HTTPOnly cookie
- [x] N-H21 — `lib/services/gasPriceService.ts` exposes `NEXT_PUBLIC_ETHERSCAN_API_KEY` to every browser
- [x] N-H22 — `NEXT_PUBLIC_PIMLICO_API_KEY` is exposed to clients with no domain restriction visible in code
- [x] N-H23 — `EmergencyControl` and `EmergencyBreaker` are nearly disconnected from the system they're supposed to halt
- [x] N-H24 — `MerchantCompetitionPool.recordTransaction` accepts attacker-supplied `volumeUsd` with no on-chain proof
- [x] N-H25 — `ServicePool.totalCommitted` over-counts unclaimed shares forever, starving future periods
- [x] N-M1 — `VFIDETestnetFaucet.batchClaim` hard-reverts whole batch on bad referrer
- [x] N-M2 — `VFIDETestnetFaucet.setEcosystemVault` lacks zero-check
- [x] N-M3 — Burn portion of fraud-flagged transfers is irreversible even on dismissal
- [x] N-M4 — `processDismissedComplaintPenalties` is opt-in / never auto-invoked
- [x] N-M5 — Permit hardcodes a literal instead of the declared `ECDSA_S_UPPER_BOUND` constant
- [x] N-M6 — `Seer.setOperatorLimits` only adjusts 3 of 5 rate-limit knobs
- [x] N-M7 — `Seer.calculateOnChainScore` trusts source-returned `weight` rather than registered weight
- [x] N-M8 — `vfide_app` DB role grants are incomplete (HIGH-severity restatement: see N-C1)
- [x] N-M9 — `users_read_authenticated` over-permissive (HIGH-severity restatement: see N-H5)
- [x] N-M10 — `FraudRegistry.clearFlag` unbounded loop (HIGH-severity restatement: see N-H1)
- [x] N-M11 — `FraudRegistry.clearFlag` doesn't reset complaint state (HIGH-severity restatement: see N-H2)
- [x] N-M12 — Escrow recipient captured at vault-address (HIGH-severity restatement: see N-H3)
- [x] N-M13 — `releaseEscrow` reverts on systemExempt removal (HIGH-severity restatement: see N-H4)
- [x] N-M14 — `EcosystemVault.creditUserReferral` and `creditMerchantReferral` perform no on-chain check of the threshold they claim to enforce
- [x] N-M15 — `Seer.calculateOnChainScore` source weight (duplicate restatement: see N-M7)
- [x] N-M16 — `Seer.setOperatorLimits` 3-of-5 knobs (duplicate restatement: see N-M6)
- [x] N-M17 — `TerminalRegistry.registerTerminal` first-come-first-served on hardware IDs with no proof of possession
- [x] N-M18 — `MerchantPortal.completeRefund` requires merchant EOA approval (incompatible with vault custody)
- [x] N-M19 — `FiatRampRegistry.recordRampTransaction` allows registered providers to mint up to 70 ProofScore per user with no on-chain proof
- [x] N-M20 — `Seer.calculateOnChainScore` weight trust (duplicate of N-M7/N-M15)
- [x] N-M21 — `AdminMultiSig.communityVeto` requires only `vetoMinScore = 5000` (default new-vault score)
- [x] N-M22 — `FeeDistributor` has no rescue/sweep for stranded tokens when a sink reverts
- [x] N-M23 — `VFIDETermLoan.extractFromGuarantors` daily timer consumed even when no extraction succeeded
- [x] N-M24 — Faucet operator key in `process.env.FAUCET_OPERATOR_PRIVATE_KEY` enables 50K-VFIDE/day theft on key leak
- [x] N-M25 — `VFIDEFinance.setModules` does two-step DAO rotation but immediately changes `ledger` and `vfideToken`
- [x] N-M26 — `AdminMultiSig.communityVeto` (duplicate of N-M21)
- [x] N-M27 — `MainstreamPriceOracle._getUniswapPrice` permanently disabled; manual fallback is the only fallback
- [x] N-M28 — `VFIDEAccessControl.BLACKLIST_MANAGER_ROLE` is dead code (contradicts H-30 "ACCEPTED RISK")
- [x] N-M29 — `SeerAutonomous.beforeAction` is wired only into DAO governance, not the token-transfer path
- [x] N-M30 — `VaultInfrastructure.UserVaultLegacy.notLocked` modifier is a no-op
- [x] N-M31 — `CouncilElection.vote` reads `seer.getScore(msg.sender)` at vote-time rather than snapshotting at election-start
- [x] N-M32 — `CouncilSalary.distributeSalary` pays in VFIDE despite Howey-compliance commitment to "auto-swap to ETH/USDC"
- [x] N-M33 — `CouncilSalary.setDAO` is instant, no timelock
- [x] N-M34 — `VFIDEEnterpriseGateway._swapToStable` slippage protection is tautological by default
- [x] N-M35 — `BridgeSecurityModule` retains explicit blacklist/clear-flags admin functions, contradicting non-custodial removal
- [x] N-M36 — `CouncilSalary.distributeSalary` pays in VFIDE (duplicate of N-M32)
- [ ] N-M37 — `SeerWorkAttestation` is built but disconnected from the pools that pay people
- [x] N-M38 — `PayrollManager.emergencyWithdraw` lets DAO drain any stream to any address with no timelock and no participant consent
- [x] N-M39 — `ProofLedger.setDAO` and `setLogger` are instant, no timelock; captured DAO can pollute the immutable event log
- [x] N-M40 — `app/api/remittance/beneficiaries/route.ts` collects PII contradicting the public privacy claim
- [x] N-L1 — `VFIDETestnetFaucet.batchClaim` whole-batch revert (duplicate of N-M1)
- [x] N-L2 — `setEcosystemVault` zero-check (duplicate of N-M2)
- [x] N-L3 — burn portion irreversible (duplicate of N-M3)
- [x] N-L4 — `processDismissedComplaintPenalties` opt-in (duplicate of N-M4)
- [x] N-L5 — Permit hardcoded literal (duplicate of N-M5)
- [x] N-L6 — POST `/api/auth` returns JWT in JSON body (HIGH-severity restatement: see N-H20)
- [x] N-L7 — Step-up failure leaks internal `error.message`
- [x] N-L8 — `/api/users/invite` enumeration oracle (mitigated)
- [x] N-L9 — Modulo bias in invite-code RNG
- [x] N-L10 — `/api/community/stories` is unauthenticated
- [x] N-L11 — `formatAsPDF` returns `Content-Type: text/html`
- [x] N-L12 — `MerchantPortal._transferWithAutoConvert` silent downgrade
- [x] N-L13 — `SubscriptionManager.processPayment` does not check fraud status
- [x] N-L14 — `VFIDEBridge._lzReceive` does not check fraud status of receiver
- [ ] N-L15 — `SessionKeyManager` is a complete, unintegrated contract
- [ ] N-L16 — Websocket server has `broadcast` exported but no caller
- [x] N-L17 — `SeerGuardian.setDAO` instant rotation, no timelock
- [x] N-L18 — `CommerceEscrow.dispute` lacks `nonReentrant` while peer functions have it
- [ ] N-L19 — `DAOTimelock.execute` admin-only; secondary executor recovery requires `secondaryExecutor` rotation runbook
- [ ] N-L20 — `app/api/messages/route.ts` validates the `sig` field structurally only; never ECDSA-recovers
- [ ] N-L21 — `app/api/messages/route.ts` replay-detection only catches identical-content
- [x] N-L22 — `DutyDistributor.maxPointsPerUser = 10_000` and `pointsPerVote = 10` cap users at 1000 lifetime votes
- [ ] N-L23 — `VFIDEFinance.noteVFIDE` accepts a self-reported amount with no on-chain verification
- [ ] N-L24 — `VFIDEBadgeNFT` is soulbound with no burn path; lost-wallet badges unreachable
- [x] N-L25 — `SeerGuardian.setDAO` instant rotation (duplicate of N-L17)
- [x] N-L26 — `CommerceEscrow.dispute` lacks `nonReentrant` (duplicate of N-L18)
- [ ] N-L27 — `lib/auth/middleware.requireAdmin` rejects users matching `OCP.owner()` but not in `ADMIN_ADDRESSES` env
- [ ] N-L28 — `ADMIN_ADDRESSES` loaded once at module init; rotation requires process restart
- [ ] N-L29 — `app/api/pos/charge/route.ts` returns `chargeId = pos_${Date.now()}` and persists nothing
- [ ] N-L30 — `app/api/seer/analytics/rollup/route.ts` is gated behind double-check `requireAdmin`
- [ ] N-L31 — `CouncilSalary.currentTerm` only incremented manually via `startNewTerm()` (DAO-only)
- [ ] N-L32 — `components/seo/StructuredData.tsx::FAQSchema` does not pass `faq.question`/`faq.answer` through `sanitizeJsonLdString`
- [ ] N-L33 — `lib/postQuantumEncryption.ts` keeps PQ private keys in process memory only
- [ ] N-L34 — `app/api/privacy/delete/route.ts` only queues a deletion request; no automatic deletion happens
- [ ] N-L35 — `SeerPolicyGuard.setDAO` is instant while `setSeer` is two-step migration; inconsistent
- [ ] N-L36 — `SystemHandover.executeHandover` doesn't actually call `setAdmin` on DAO/Timelock; relies on dev pre-configuring via governance
- [ ] N-L37 — `app/api/auth/route.ts` uses `verifyMessage` from `viem` which accepts EIP-1271 contract-wallet signatures
- [ ] N-L38 — `ServicePool.emergencyWithdraw` is `DEFAULT_ADMIN_ROLE` only and bypasses `pause`/`unpause`
